// STUB for the a2-crossing sandbox (replaces ~/Game src/net/room.ts).
// The crossing experiment never joins a multiplayer room.
export function isInRoom(): boolean {
  return false;
}
export function broadcastBasket(_who: unknown): void {}
