// STUB for the a2-crossing sandbox (replaces ~/Game src/components/hero/Interior10600.tsx).
// The crossing experiment only ever sees the hero house's EXTERIOR (backyard at
// ground level, then a flyover); the full interior pulls in the projector
// screen / kitchen / bedrooms subtree, which is out of scope for this sandbox.
export function Interior10600(_props: {
  depth: number;
  doorCenterX: number;
  garageCenterX: number;
}) {
  return null;
}
